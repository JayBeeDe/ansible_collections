- hosts: all
  gather_facts: true
  collections:
    - jaybeede.linux_desktop
  tasks:
    # - import_role:
    #     name: packages
    # - import_role:
    #     name: system
    # - import_role:
    #     name: applications
    - import_role:
        name: desktop
    # - import_role:
    #     name: gnome
